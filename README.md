# agent-based-fire-spread
GT 8900 Research Project developing an agent based fire spread model to validate and test against FlamMap. 

To run the simulation, simply clone this repository, create a virtual environment and install requirements.txt. From here, you can run main.py to run the fire simulation and comparison.py to compare the outputs of this ABM against FlamMap outputs. 